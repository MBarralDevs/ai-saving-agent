{
  "name": "pigment",
  "version": "1.0.0",
  "description": "Pigment - AI-powered automated DeFi savings. Drop by drop, build your financial masterpiece.",
  "private": true,
  "workspaces": [
    "backend"
  ],
  "scripts": {
    "test:contracts": "cd contracts && forge test -vvv",
    "build:contracts": "cd contracts && forge build",
    "test:backend": "cd backend && npm test",
    "dev:backend": "cd backend && npm run dev"
  },
  "dependencies": {
    "axios": "^1.13.2"
  }
}
